# Release 19: Rename Simple → Tiny

## Overview
Rename the language and interpreter from "Simple Interpreter" to "Tiny Development Environment" across the entire codebase.

## Scope Analysis
- **Total occurrences**: 84 (69 capitalized "Simple", 15 lowercase "simple")
- **Files affected**: 37

---

## Phased Execution Plan

### Phase 1: Tests First (Safest)
Update test assertions so they expect "Tiny" - tests will fail until HTML is updated.

**Files:**
- [x] `tests/ui.spec.js`

**Exact changes:**
```
Line 7:
-  await expect(page).toHaveTitle('Simple Interpreter');
+  await expect(page).toHaveTitle('Tiny Development Environment');
```

**Expected result:** Tests will FAIL after this phase until Phase 3 (HTML) is complete.

**Status:** COMPLETE (commit 8b17a1d)

---

### Phase 2: Core Code (Functional)
Rename the `SimpleFunction` class to `TinyFunction`. This is load-bearing code.

**Files:**
- [x] `src/runtime.js` - class definition and export
- [x] `src/interpreter.js` - class usage (2 references)

**Status:** COMPLETE (commit 95cff7e) - All function/closure tests pass

---

### Phase 3: User-Facing HTML (Visible)
Update titles, headers, and user-visible text.

**Files:**
- [x] `index.html` - `<title>` and `<h1>`
- [x] `language-help.html` - `<title>`, `<h1>`, footer, language description text

**Status:** COMPLETE (commit d907906)

---

### Phase 4: Package Identity
Update package metadata and build scripts.

**Files:**
- [x] `package.json` - `"name": "simple-interpreter"` → `"name": "tiny"`
- [x] `package-lock.json` - name fields (2 locations)
- [x] `build.sh` - echo message

**Status:** COMPLETE

---

### Phase 5: Source File Comments (Cosmetic)
Update header comments in all source files.

**Files (15 total):**
- [x] `src/constants.js`
- [x] `src/utils.js`
- [x] `src/lexer.js`
- [x] `src/scanner.js`
- [x] `src/parser.js`
- [x] `src/runtime.js`
- [x] `src/interpreter.js`
- [x] `src/visualizer.js`
- [x] `src/modal.js`
- [x] `src/main.js`
- [x] `src/parser-renderer.js`
- [x] `src/output-renderer.js`
- [x] `src/memory-renderer.js`
- [x] `src/examples.js`
- [x] `src/reference.js`

Note: `src/ast-renderer.js` didn't have "Simple" in header

**Status:** COMPLETE

---

### Phase 6: Example Programs (Cosmetic)
Update example names and code comments.

**Files:**
- [x] `src/examples/pong.js` - name and code comment
- [x] `src/examples/calculator.js` - description and code comment
- [x] `src/examples/control-flow.js` - code comment
- [x] `src/examples/functions.js` - code comment

**Status:** COMPLETE

---

### Phase 7: Documentation (Historical - Optional)
Update project documentation. Release notes are historical records, so renaming there is optional.

**Files:**
- [ ] `CLAUDE.md` - project title
- [ ] `releases/*.md` (18 files) - Optional: these are historical records

---

## Execution Notes

1. Git commit and push after each phase
2. Run tests after each phase to verify nothing breaks
3. Phase 2 (SimpleFunction → TinyFunction) is the most critical - if anything breaks, it's here
4. Phases 5-7 are cosmetic and low-risk

## Progress

- [x] Phase 1: Tests (commit 8b17a1d)
- [x] Phase 2: Core Code (commit 95cff7e)
- [x] Phase 3: HTML (commit d907906)
- [x] Phase 4: Package (commit 2eafce0)
- [x] Phase 5: Comments (commit e445532)
- [x] Phase 6: Examples
- [ ] Phase 7: Documentation
